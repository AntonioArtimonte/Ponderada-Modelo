import React from 'react';
import Link from 'next/link';
import { ListItemText, Divider, ListItemButton } from '@mui/material'; // Use ListItemButton from Material-UI

const SearchBar = () => {
  return (
    <div>
    </div>
  );
};

export default SearchBar;
